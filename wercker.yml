build: 
  box: golang
  steps:
    - script:
        name: Delete /pipeline
        code: |
          rm -rf /pipeline
  after-steps:
    - script:
        name: Say hello
        code: |
          echo Hello    
